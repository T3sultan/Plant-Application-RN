import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Box = () => {
  return (
    <View>
      <Text>Box</Text>
    </View>
  );
};

export default Box;

const styles = StyleSheet.create({});
